模块名称：控制器模块（controller）
模块职责：作为「视图」与「数据访问层」的中间层，负责连接界面交互与业务逻辑，处理用户操作事件。
包含类：
- LoginController：负责登录界面（LoginFrame）的事件处理，比如「登录按钮点击」逻辑：
  1. 接收登录界面的用户名/密码输入；
  2. 调用ProductDao验证用户合法性；
  3. 验证成功后跳转主界面，失败则提示错误。
核心功能：
- 绑定视图的交互事件（如按钮点击、输入框提交）；
- 调用Dao层方法完成数据操作；
- 处理操作结果并反馈到视图（如提示消息、跳转界面）。