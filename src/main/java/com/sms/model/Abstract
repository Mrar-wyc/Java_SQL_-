模块名称：实体模型模块（model）
模块职责：对应数据库中的表结构，封装业务数据，作为数据在各模块间传递的载体。
包含类：
- Category：对应product_category表，封装商品分类数据（分类ID、分类名称）；
- Product：对应product表，封装商品核心数据（商品ID、名称、分类、价格、库存等）；
- Supplier：对应supplier表，封装供应商数据（供应商ID、名称、联系方式）；
- Warehouse：对应warehouse表，封装仓库数据（仓库ID、名称、地址）。
核心功能：
- 每个类的属性与数据库表字段一一对应；
- 提供getter/setter方法，实现数据的封装与读取；
- 作为「Dao层查询结果」与「View层展示数据」的中间载体。