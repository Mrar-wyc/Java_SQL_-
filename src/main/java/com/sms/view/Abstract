模块名称：视图模块（view）
模块职责：负责界面展示与用户交互，提供可视化操作入口，展示业务数据。
包含类：
- LoginFrame：登录界面，提供用户名/密码输入框、登录按钮，是系统的入口界面；
- MainFrame：主操作界面，包含以下核心元素：
  1. 搜索栏：搜索商品（名称/分类/条形码）；
  2. 功能按钮：新增/修改/删除商品、多条件筛选、销售出库等；
  3. 商品表格：展示商品数据，支持选中行操作。
核心功能：
- 提供用户操作的可视化控件（按钮、输入框、表格、下拉框）；
- 展示Dao层查询到的业务数据（如商品列表）；
- 接收用户输入并触发控制器的事件处理逻辑。