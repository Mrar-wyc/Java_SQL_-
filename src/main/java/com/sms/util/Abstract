模块名称：工具类模块（util）
模块职责：提供项目通用的工具方法，封装重复逻辑，减少代码冗余。
包含类：
- DbUtil：负责数据库相关的通用工具：
  1. 获取数据库连接：通过JDBC驱动连接MySQL数据库；
  2. 关闭数据库资源：统一关闭Connection、PreparedStatement、ResultSet，避免资源泄露。
核心功能：
- 封装通用、无业务侵入性的工具方法；
- 简化其他模块的代码（如Dao层直接调用DbUtil获取连接）；
- 保证通用逻辑的一致性（如所有数据库连接的URL、账号配置统一在DbUtil中）。